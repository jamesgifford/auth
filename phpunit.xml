<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="random"
         failOnWarning="true"
         failOnRisky="true"
         failOnEmptyTestSuite="true"
         beStrictAboutOutputDuringTests="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <!-- MariaDB is the package's real target; the suite runs against it by
             default. These only apply when the variable is not already set, so
             CI (or a one-off run) overrides them from the environment; use
             `composer test:sqlite` for the fast in-memory loop. -->
        <env name="DB_CONNECTION" value="mariadb"/>
        <env name="DB_DATABASE" value="jamesgifford_auth_test"/>
        <env name="DB_USERNAME" value="jamesgifford"/>
        <env name="DB_SOCKET" value="/tmp/mysql.sock"/>
    </php>
</phpunit>
